# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# venvs / tooling
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.ipynb_checkpoints/
*.log

# editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# machine-local config: interpreter path, model paths, API key.
# `assets/demars.yaml.example` is the tracked template; `.claude/settings.json` is tracked,
# the per-machine permission overrides beside it are not.
demars.yaml
.claude/settings.local.json

# run products -- large, machine-specific, regenerated by the pipeline
runs/
tmp/
